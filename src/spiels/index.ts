const itpv = `ITPV stands for "Item Trading Platform Value" is a way to
                quantify the value of the trading item. if does not have any
                binding in any standard currency which is constant fluctuation
                of value and subject to inflation. ITPV aims to quantify the
                item without bias in any situation at any location. the current
                proposal in valuation of the item is 1(one) ITPV is equal to
                1(one) apple`;

const spiel: { itpv: string } = {
  itpv,
};

export default spiel;
